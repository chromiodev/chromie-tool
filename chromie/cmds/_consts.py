# Embedding function names. [0] is the default value if needed.
EMBEDDING_FNS = ("default", "sentence_transformer")

# Space names. [0] is the default value if needed.
HNSW_SPACES = ("cosine", "ip", "l2")
